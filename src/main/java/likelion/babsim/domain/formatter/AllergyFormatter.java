package likelion.babsim.domain.formatter;

import java.util.List;

public class AllergyFormatter {

    /*public static List<Long> parseAllergyList(String allergiesStr) {
        List<Long> allergiesIdList = new ArrayList<>();
        String[] allergyNames = allergiesStr.split(","); // 문자열을 쉼표로 분리하여 각 알러지명 을 가져옴
        for (String name : allergyNames) {
            name = name.trim(); // 공백 제거
            if (!name.isEmpty()) {
                allergiesIdList.add();
            }
        }
        return allergiesIdList;
    }*/
    /*public static String formatAllergyList(List<Long> allergyList) {
        return String.join(",", allergyList);
    }*/
}
